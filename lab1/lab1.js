function triangle (value1, type1, value2, type2) {
    let leg, hypotenuse, adjacent_angle, opposide_angle, angle;
    let a,b,c, alpha, beta;

    if((type1 === "leg") && (type2 === "leg")){
        

    }
    console.log(value1, type1, value2, type2);
}
